services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - intent-data:/app/data
    environment:
      DATABASE_URL: sqlite:////app/data/intent_signals.db
    restart: unless-stopped

volumes:
  intent-data:
